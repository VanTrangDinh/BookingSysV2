import { HostId } from '../repositories/host';
import { ListingId } from '../repositories/listing/types';

// export type EnforceHostId = { _hostId: HostId };
// export type EnforceListingId = { _listingId: ListingId };

// neu khong co fields can query nam torng model thi ta tao type nay
// export type EnforceEnvOrOrgIdsV2 = EnforceHostId;
